coverage/
dist/
docs/
proxmox-/
README.md

# Vendored upstream installation manifests. These are byte-for-byte copies of pinned
# upstream releases; reformatting them would break the provenance check in
# tools/kubernetes/bootstrap.mjs, which compares them against the recorded digest.
deploy/kubernetes/bootstrap/argocd/install.yaml
deploy/kubernetes/bootstrap/argo-rollouts/install.yaml

# Generated by the package manager. Prettier reformatting a lockfile makes every install produce a
# spurious diff, and the file is not read by a human.
pnpm-lock.yaml

# Editor and agent tooling checked in by contributors' local setups. Not this project's source, and
# reformatting them churns files nobody reviews.
.agents/
.claude/
.cursor/
