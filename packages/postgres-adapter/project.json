{
  "name": "postgres-adapter",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/postgres-adapter/src",
  "projectType": "library",
  "tags": ["type:lib", "layer:adapter", "visibility:production"],
  "targets": {
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.integration.config.ts",
        "cwd": "packages/postgres-adapter"
      },
      "cache": false,
      "dependsOn": ["^build"]
    }
  }
}
